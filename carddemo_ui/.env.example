# FastAPI backend URL (default: http://localhost:8000)
NEXT_PUBLIC_API_URL=http://localhost:8000

# App environment
NODE_ENV=development

# Optional: override API base path if hosted under a subpath
# NEXT_PUBLIC_API_BASE_PATH=/api/v1
